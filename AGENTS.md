# Internship Hunter Agent Instructions

- Run `npm run lint` before finalizing.
- Run `npm run build` before finalizing when possible.
- Keep API keys server-side only.
- Never expose `OPENAI_API_KEY` in frontend code.
- Do not scrape LinkedIn.
- Do not build manual offer entry as the core workflow.
- Keep code simple and MVP-oriented.
- Prefer small incremental changes.
- Keep the product focused on automated AI-powered internship search.
- Use mock fallbacks when external services are not configured.
